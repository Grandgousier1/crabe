// Configuration chargeable côté client. Modifiez `apiEndpoint` avant déploiement.
window.DNT_CONFIG = {
  apiEndpoint: "https://crabe.onrender.com/transform",
  // Optionnel : changez le modèle Gemini.
  model: "gemini-flash-latest",
};
